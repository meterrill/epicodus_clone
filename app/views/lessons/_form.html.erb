<%= render('layouts/errors', :object => @lesson) %>

<%= form_for @lesson do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :text %>
  <%= f.text_area :text %>
  <p><%= f.submit %></p>
<% end %>
